# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/release.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  changelog:
    runs-on: ubuntu-latest
    steps:
    - name: checkout full
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        fetch-depth: 0
    - env:
        OUTPUT: CHANGELOG.md
      name: Generate a changelog
      uses: orhun/git-cliff-action@c93ef52f3d0ddcdcc9bd5447d98d458a11cd4f72
      with:
        args: --verbose --current
        config: cliff.toml
    - name: Upload changelog
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
      with:
        name: changelog
        path: CHANGELOG.md
        retention-days: 1
  release:
    needs:
    - changelog
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
    - name: Install nix
      uses: cachix/install-nix-action@4e002c8ec80594ecd40e759629461e26c8abed15
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: Generate Version
      run: |-
        GITHUB_TAG_NAME=${{ github.ref_name }}
        TAG_NAME=${GITHUB_TAG_NAME:-v0.0.0}
        TAG_VERSION=${TAG_NAME: 1}
        echo "TAG_VERSION=$TAG_VERSION" >> $GITHUB_ENV
    - name: Get changelog
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
      with:
        name: changelog
        path: changelog
    - name: Build
      run: |-
        cat package_base.json | jq '.version = $TAG_VERSION'
        nix build .
    - name: Publish artifacts and release
      uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b
      with:
        body_path: changelog/CHANGELOG.md
        files: result/grustonnet.vsix
'on':
  push:
    tags:
    - v*
  workflow_dispatch: {}
